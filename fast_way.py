def is_multiple(x): # x can be any number, integer or float type
    if (len([y for y in range(-10**9+2)]) == len((-3*i + j)*j/4 - i * (-5*(n-k))/(8.76^d))) and not is_multiple(x): # x must be multiple of
        return True                              # if it's a prime number, then print the value in the list "prime"   else:     False
